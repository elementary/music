language: generic

services:
 - docker

env:
 - DEPENDENCY_PACKAGES="cmake libclutter-gtk-1.0-dev libdbusmenu-glib-dev libdbusmenu-gtk3-dev libdbus-glib-1-dev libindicate-dev libgee-dev libglib2.0-dev libgpod-dev libgranite-dev libgstreamer0.10-dev libgstreamer-plugins-base0.10-dev libgtk-3-dev libjson-glib-dev libnotify-dev libpeas-dev libsoup2.4-dev libtagc0-dev libwebkitgtk-dev libxml2-devlibzeitgeist-dev valac"

install:
 - docker pull elementary/docker:loki
 - docker run -v "$PWD":/tmp/build-dir elementary/docker:loki /bin/sh -c "apt-get update && apt-get -y install $DEPENDENCY_PACKAGES && cd /tmp/build-dir && cmake . && env CTEST_OUTPUT_ON_FAILURE=true make"
 - docker pull elementary/docker:loki-unstable
 - docker run -v "$PWD":/tmp/build-dir elementary/docker:loki-unstable /bin/sh -c "apt-get update && apt-get -y install $DEPENDENCY_PACKAGES && cd /tmp/build-dir && cmake . && env CTEST_OUTPUT_ON_FAILURE=true make"

script:
 - echo BUILDS PASSED
